import App from '../../App'
import { mount } from '@vue/test-utils'

test('Verificar criação do card usuário', ()=>{
    const nome = 'roniere';
    const wrapper = mount(App, {
        propsData: {
        }
    })

})
